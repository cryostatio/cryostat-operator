resources:
- ../../manifests
- ../../openshift

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
patches:
- path: console_plugin_patch.yaml
  target:
    group: apps
    kind: Deployment
    name: controller
    namespace: system
    version: v1
