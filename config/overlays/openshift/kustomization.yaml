apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../manifests
- ../../openshift

patches:
- path: console_plugin_patch.yaml
  target:
    group: apps
    version: v1
    kind: Deployment
    name: controller
    namespace: system
